export const metadata = {
  title: 'Leaderboard',
  description: 'Check out the top-performing players and organizations on TournaDash. Track wins, points, and event placements.',
}

export default function LeaderboardLayout({ children }) {
  return <>{children}</>
}
