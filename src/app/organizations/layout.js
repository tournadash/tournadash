export const metadata = {
  title: 'Browse Organizations',
  description: 'Discover Minecraft tournament organizations on TournaDash. Follow hosts, view events, and join communities.',
}

export default function OrganizationsLayout({ children }) {
  return <>{children}</>
}
